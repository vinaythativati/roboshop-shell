- name: nginx install and start
  hosts: forntend
  become: yes
  tasks:
    - name: install nginx
      package:
        name: nginx
        state: present

    - name: start nginx
      service:
        name: starting nginx
        state: started
        enabled: yes 